import { createFileRoute, Link } from "@tanstack/react-router";
import { Button } from "@/components/ui/button";
import { EmptyState } from "@/components/marketplace/feedback-states";
export const Route = createFileRoute("/product/$slug")({ head: ({ params }) => ({ meta: [{ title: `${params.slug} — Modalia` }, { name: "description", content: "Explore this product on Modalia." }, { property: "og:title", content: `${params.slug} — Modalia` }, { property: "og:description", content: "Explore this product on Modalia." }, { property: "og:type", content: "product" }, { name: "twitter:card", content: "summary_large_image" }], links: [{ rel: "canonical", href: `/product/${params.slug}` }] }), component: ProductPage });
function ProductPage() { const { slug } = Route.useParams(); return <EmptyState title={slug}>Product pages will be populated by the managed catalogue.<div className="mt-7"><Button asChild variant="outline"><Link to="/shop" search={{ locale: "fr", q: "", category: "", sort: "newest", page: 1, focus: "", view: "" }}>Back to shop</Link></Button></div></EmptyState>; }
