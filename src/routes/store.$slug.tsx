import { createFileRoute, Link } from "@tanstack/react-router";
import { Button } from "@/components/ui/button";
import { EmptyState } from "@/components/marketplace/feedback-states";
export const Route = createFileRoute("/store/$slug")({ head: ({ params }) => ({ meta: [{ title: `${params.slug} — Modalia` }, { name: "description", content: "Explore this independent store on Modalia." }, { property: "og:title", content: `${params.slug} — Modalia` }, { property: "og:description", content: "Explore this independent store on Modalia." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }], links: [{ rel: "canonical", href: `/store/${params.slug}` }] }), component: StorePage });
function StorePage() { const { slug } = Route.useParams(); return <EmptyState title={slug}>Store pages will be populated by approved seller data.<div className="mt-7"><Button asChild variant="outline"><Link to="/shop">Back to shop</Link></Button></div></EmptyState>; }
